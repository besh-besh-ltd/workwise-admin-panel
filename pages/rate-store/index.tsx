import React from "react";
import RateStoreList from "@/components/rate-store";

const RateStorePage: React.FC = () => {
  return (
    <div>
      <RateStoreList />
    </div>
  );
};

export default RateStorePage;
